# A shell script with CRLF fails on Linux with "bad interpreter", and systemd
# will not parse a unit file with CRLF. Keep these LF in every working copy,
# whatever core.autocrlf says locally.
*.sh text eol=lf
*.service text eol=lf
